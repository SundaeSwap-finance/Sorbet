export enum EView {
  DEBUG = "debug",
  OVERRIDE = "override",
  ADDRESS_BOOK = "addressbook",
}

export enum EWalletType {
  OVERRIDE = "override",
  WRAP = "wrap",
  IMPERSONATE = "impersonate",
}
